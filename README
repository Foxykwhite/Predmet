pip install dash pandas requests
python apps.py
cd ..
pip install django
pip install djangorestframework
pip install drf_yasg
python manage.py runserver
# Front
npm install bootstrap recharts react-router-dom
npm run dev
# Docker
docker-compose up --build -d
docker-compose down
# Playwright
pip install playwright
playwright install
python test_playwright.py
# Locust
pip install locust
locust -f locustfile.py --host=https://effective-cod-wq4gqvrp6p9cgpxg.github.dev
# Docs
pip install sphinx
pip install sphinx-autodoc-typehints
pip install sphinxcontrib-django
make html
# Mc Docs
pip install mkdocs-material
pip install mkdocs
mkdocs serve